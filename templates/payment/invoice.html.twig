<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice</title>
    <style>
body, h1, h2, h3, p, ul, li {
    margin: 0;
    padding: 0;
}


* {
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
    color: #333;
    font-size: 10px; /* Reduce font size for compactness */
}

/* Invoice container styles */
.invoice {
    max-width: 420px; /* Adjust max-width for A7 paper size */
    margin: 5px auto; /* Adjust margin for centering on smaller paper */
    padding: 5px;
    background-color: #fff;
    border: 1px solid #ccc;
    border-radius: 3px;
}

/* Invoice header styles */
.invoice-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid #ccc;
    padding-bottom: 5px;
    margin-bottom: 10px;
}

/* Invoice title styles */
.invoice-header h2 {
    margin: 0;
    font-size: 18px; /* Reduce font size for compactness */
}

/* Invoice details styles */
.invoice-details p {
    margin: 0;
    font-size: 12px; /* Reduce font size for compactness */
}

/* Invoice details label styles */
.invoice-details strong {
    font-weight: bold;
}

/* Invoice body styles */
.invoice-body {
    /* Add your styles here */
}

/* Invoice item styles */
.item {
    margin-bottom: 5px;
}

/* Invoice total styles */
.invoice-total {
    border-top: 1px solid #ccc;
    padding-top: 5px;
    margin-top: 10px;
    font-size: 12px; /* Reduce font size for compactness */
}

/* Invoice footer styles */
.invoice-footer {
    margin-top: 10px;
    font-size: 10px; /* Reduce font size for compactness */
}
h4 {
  position: relative;
  padding: 0;
  margin: 0;
  font-family: "Raleway", sans-serif;
  font-weight: 300;
  font-size: 28px;
  color: #080808;
  -webkit-transition: all 0.4s ease 0s;
  -o-transition: all 0.4s ease 0s;
  transition: all 0.4s ease 0s;
}

h4 span {
  display: block;
  font-size: 0.5em;
  line-height: 1.3;
}
h4 em {
  font-style: normal;
  font-weight: 600;
}

.seven h4 {
text-align: center;
margin: 15px;
    font-size:30px; font-weight:300; color:#222; letter-spacing:1px;
    text-transform: uppercase;
    display: grid;
    grid-template-columns: 1fr max-content 1fr;
    grid-template-rows: 27px 0;
    grid-gap: 20px;
    align-items: center;
}

.seven h4:after,.seven h4:before {
    content: " ";
    display: block;
    border-bottom: 2px solid #3f96cd;
    border-top: 2px solid #3f96cd;
    height: 5px;
  background-color:#f8f8f8;
}

    </style>
</head>
<body>
    <div class="invoice">
        <div class="invoice-header">
             <div class="seven">
        <h4>invoice</h4>
      </div>
            <div class="invoice-details">
                <p><strong>Full Name :</strong>{{CardOwner}} {{CardOwnerLast}}</p>
                <p><strong>Date:</strong>{{ payment.PaymentDate|date('Y-m-d') }}</p>
            </div>
        </div>
        <div class="invoice-body">
            <div class="invoice-items">
                {% set total = 0 %}
                {% for product in products %}
                <div class="item">
                    <p>Product Name : <strong>{{ product.name }}</strong></p>
                    <p>Quantity: {{ product.StockQu }}</p>
                    <p>Price: {{ product.price }}</p>
                    <p>Total: {{ product.price * product.StockQu }}</p>
                </div>
                {% set total = total + (product.price * product.StockQu) %}
                {% endfor %}
            </div>
            <div class="invoice-total">
                <p><strong>Total</strong>: {{ total }}</p>
            </div>
        </div>
        <div class="invoice-footer">
            <p>Thank you for your business!</p>
            <p>For any inquiries, contact our customer support.</p>
        </div>
    </div>
</body>
</html>
