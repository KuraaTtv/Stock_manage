
<nav class="sidebar sidebar-offcanvas fixed-left" id="sidebar">
  <ul class="nav">
    <li class="nav-item">
      <a class="nav-link" href="{{ path('app_test')}}">
        <i class="mdi mdi-grid-large menu-icon"></i>
        <span class="menu-title fs-6">Dashboard</span>
      </a>
    </li>
    <ul class="nav">
  {% for Model in Models() %}
    {% if "ROLE_ADMIN" in Model.role %}
      <li class="nav-item">
        <a class="nav-link collapsed" data-bs-toggle="collapse" 
           href="#admin-ui-basic-{{ loop.index }}" 
           aria-expanded="false" 
           aria-controls="admin-ui-basic-{{ loop.index }}">
          <i class="m-3 {{ Model.icon }}"></i>
          <span class="menu-title fs-6">{{ Model.title }}</span>
          <i class="menu-arrow"></i>
        </a>
        <div class="collapse" id="admin-ui-basic-{{ loop.index }}">
          <ul class="nav flex-column sub-menu">
            <li class="nav-item">
              <a class="nav-link" href="{{ path(Model.path) }}">{{ Model.name }}</a>
            </li>
          </ul>
        </div>
      </li>
      {% endif %}
{# {% if "ROLE_USER" in Model.role %}
      <li class="nav-item">
        <a class="nav-link collapsed" data-bs-toggle="collapse" 
           href="#admin-ui-basic-{{ loop.index }}" 
           aria-expanded="false" 
           aria-controls="admin-ui-basic-{{ loop.index }}">
          <i class="m-3 {{ Model.icon }}"></i>
          <span class="menu-title fs-6">{{ Model.title }}</span>
          <i class="menu-arrow"></i>
        </a>
        <div class="collapse" id="admin-ui-basic-{{ loop.index }}">
          <ul class="nav flex-column sub-menu">
            <li class="nav-item">
              <a class="nav-link" href="{{ path(Model.path) }}">{{ Model.name }}</a>
            </li>
          </ul>
        </div>
      </li>
      {% endif %} #}


{% endfor %}
</ul>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>
</nav>